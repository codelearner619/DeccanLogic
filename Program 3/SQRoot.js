const readline = require('readline');     const r1 = readline.createInterface(    process.stdin, process.stdout);   r1.question('Enter the number ', (n) => {  let ans = findSqrt(n);    if (Math.floor(ans) == ans )   {       console.log(ans + " is the square root of the given number");          }else{    console.log(ans.toPrecision(4) + " is the square root of the given number");   } r1.close(); return; process.exit(0);});    	function findSqrt(x){		if(x < 2){			return x;		}		let y = x;		let z = (y + (x/y))/2;				while(Math.abs(y-z)>=0.00001){			y = z;			z = (y + (x/y))/2;		}		return z;	}